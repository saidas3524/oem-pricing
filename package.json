{
  "name": "oem-pricingcapability",
  "version": "2.0.0",
  "description": "An example module demonstrating how to share react components",
  "main": "build/index.js",
  "author": "sai krishna ",
  "license": "MIT",
  "dependencies": {
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1"
  },
  "scripts": {
    "clean": "rimraf build",
    "build": "yarn run clean && babel src -d build --copy-files",
    "prepare": "yarn run build"
  }
}
